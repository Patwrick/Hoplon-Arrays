# Repository maintenance

- First prepared snapshot: `VERSION` identifies release 0.1.0; mechanical revision remains rev7. `docs/release-notes.md` and `CHANGELOG.md` describe a prototype source release with an incomplete printable mold set. They do not establish that publication or tagging occurred. Keep deferred work visible without promoting it to tested behavior.
- Owner-approved purpose: under-desk cable routing and management. The panel back mounts to the desk underside with adhesive; units face downward. Owner-used material links and product-documentation gaps are in `docs/build-guide.md`. Panel-back adhesive-fitting slits are planned, absent from current CAD; do not invent dimensions or implement them during documentation work. A future demonstration video is tracked in `docs/photos.md`.
- Canonical mechanical source: `system_rev7_perforated_score.scad` at the root. Millimeters. Default 4 × 4 configuration; all scoring enables are false. See `docs/parameters.md` and `docs/validation.md` for hashes, dimensions and export status.
- Preserve original pad, column and eight support fins. Four smaller ribs belong to the post. The rigid panel is separate from the three-piece mold. Do not restore removed receivers, carriers, clips, ejector blocks or add nut traps.
- Never silently change mechanical geometry, source defaults, or original design files while editing documentation. Describe a defect, affected files, proposed change and validation plan for owner review. Boolean union overlaps and preview artifacts are not automatically invalid final solids.
- `silicone_positive` is an unsocketed cavity envelope, not a finished silicone part. Shared `ball_snap_post()` cores form the socket. Source comments and exported filenames are not validation evidence.
- Regenerated candidate tooling is under `exports/rev7-default/`; inspect its manifest and checks. Preserve original root STLs and `LSR_quote_package_v1/` as local unverified references; they are excluded from the release allowlist. Never run bundled executables or unknown scripts to inspect them.
- Export: `python -B scripts/export_rev7.py --openscad openscad --output ../Hoplon-Arrays-release-review/new-candidates` (read script and dependency notes first). Inspect: `python -B scripts/inspect_stl.py exports/rev7-default/panel.stl`. Render docs: `python -B scripts/render_cad.py --openscad openscad`. These need installed OpenSCAD, Python/NumPy, and Pillow for images; do not install large tools silently. Use the recorded commands in `docs/validation.md` if script interfaces change.
- DIY material is the owner-linked BBDINO Shore 40A two-part silicone in `docs/build-guide.md`. Use product-specific ratios, density, cure, release and handling only from that product's official technical/SDS documents. A purchase link does not establish these fields or physical validation. The guide is proposed until physical records establish results. Do not reuse the old quote package's hardness or industrial process.
- `LICENSE.md` applies the owner-requested custom personal/noncommercial terms to original project materials. Business use, including internal use, and paid/for-profit use require separate permission. See `docs/licensing.md` for scope and functional-hardware limitations. Do not call it a standard CC or open-source license, invent a rights holder, relicense third-party material, or replace these terms without owner authorization.
- Keep public paths relative and identities owner-approved. Preserve license notices. Audit current files, local history, binaries, archive members and image metadata without exposing secret values. Keep originals, private backups and detailed redacted audits outside release packages. Ask before moving/deleting originals or rewriting history; ignoring/deleting current files is not a history purge.
- `docs/release-files.txt` is the explicit proposed package allowlist. Run `python -B scripts/package_review.py --check` for link and artifact-provenance checks. Build a local review ZIP outside the repository using `python -B scripts/package_review.py --output <new-outside-path.zip>`; inspect the actual archive after edits. Never bundle `.git`, legacy quote files, private audits or arbitrary untracked content. Preserve `.gitattributes` rules for source hashes; never normalize the SCAD silently.
- Preserve uncommitted work. No reset, clean, stash, commit, history rewrite, tag, push, public release or visibility change without the owner's authorization. Publication, rights/attribution review and other unresolved gates remain in `docs/release-checklist.md`.
